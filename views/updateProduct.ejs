<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href='https://fonts.googleapis.com/css?family=Roboto' rel='stylesheet'>
    <!-- <link rel="stylesheet" href="./css/home.css"> -->
    <script src="/js/home.js" defer></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        label {
            margin: 1rem;
        }

        button {
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <%- include('nav') %>
    <div class="h1 d-flex justify-content-center align-items-center m-5">Update Product</div>
    <div class="container">
        <form method="post" action="/updateP" class="d-flex flex-column justify-content-center align-items-center">
            <input type="hidden" name="id" value="<%= product[0].idproduct %>">
            <label for="" class="badge bg-secondary">Name</label>
            <input type="text" name="name" value="<%= product[0].name %>">
            <label for="" class="badge bg-secondary">Price</label>
            <input type="text" name="price" value="<%= product[0].price%>"> 
            <label for="" class="badge bg-secondary">Total</label>
            <input type="text" name="total" value=" <%= product[0].total %>"> 
            <button type="submit" class="btn btn-primary">SUBMIT</button>
        </form>
    </div>
</body>
</html>